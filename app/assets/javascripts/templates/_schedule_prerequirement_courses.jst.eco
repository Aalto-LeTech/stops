<% if @prereqs.length > 0: %>
  <ul>
    <% for course in @prereqs: %>
      <li>
        <%= course.code + " " + course.name %>
      </li>
    <% end %>
  </ul>
<% else: %>
  <%= @no_prereqs_message %>
<% end %>