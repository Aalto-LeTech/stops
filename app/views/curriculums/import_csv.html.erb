<h1><%= @curriculum.name %></h1>

<%= form_for @curriculum, :as => :curriculum, :url => curriculum_url(:id => @curriculum), :html => { :multipart => true, :method => :put } do |f| %>
  
  <table>
    <!--
    <tr>
      <td><%=t('.curriculum_prereqs_csv')%></td>
      <td><%= file_field_tag :prereqs_csv %></td>
      <td></td>
    </tr>
    
    <tr>
      <td><%=t('.curriculum_profiles_csv')%></td>
      <td><%= file_field_tag :profiles_csv %></td>
      <td></td>
    </tr>
    -->
    <tr>
      <td><%=t('.courses_csv')%></td>
      <td><%= file_field_tag :courses_csv %></td>
      <td></td>
    </tr>
  </table>
  
  <p><%= f.submit t(:Upload), :class => 'btn btn-primary' %></p>
<% end %>

<!--p><%= link_to 'Delete', @curriculum, :method => :delete %></p-->

<p><%= link_to t(:Back), curriculum_url(:id => @curriculum) %></p>
