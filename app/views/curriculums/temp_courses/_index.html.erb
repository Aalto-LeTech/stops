<table>
  <thead>
    <tr>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @temp_courses.each do |temp_course| %>
      <tr>
        <td><%= link_to temp_course.code, [@curriculum, temp_course] %></td>
        <td><%= link_to temp_course.name_fi, [@curriculum, temp_course] %></td>
        <td><%= link_to 'Edit', edit_curriculum_temp_course_path(@curriculum, temp_course) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
