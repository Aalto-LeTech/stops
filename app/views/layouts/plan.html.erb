<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
       "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="<%= I18n.locale %>" lang="<%= I18n.locale %>">
<%= render :partial => 'layouts/head' %>

<body>

<%= render :partial => 'layouts/header' %>

<div class="page">
  <!--div id="contextHelp">
  </div-->

  <div class="leftNavi">

    <% if @profile %>
      <h1><%= link_to t(:myPlanTabProfiles), studyplan_profiles_path %></h1>
      <ul>
        <% @curriculum.profiles.each do |profile| %>
          <li class="<%= profile == @profile ? 'selected' : '' %>"><%= link_to h(profile.name(I18n.locale)), studyplan_profile_path(:id => profile.id) %></li>
          <!-- Courses belonging to the profile -->
          
          <% if profile == @profile %>
            <ul>
              <% profile.courses.each do |course| %>
                <li class="<%= course == @course ? 'selected' : '' %>"><%= link_to h(course.name(I18n.locale)), studyplan_profile_course_path(:profile_id => profile.id, :id => course.id) %>
              <% end %>
            </ul>
          <% end %>
        <% end %>
      </ul>
    <% elsif @course %>
      <h1><%= link_to t(:plan_my_courses), studyplan_courses_path %></h1>
      <ul>
        <% @user.courses.each do |course| %>
          <li class="<%= course == @course ? 'selected' : '' %>"><%= link_to h(course.name(I18n.locale)), curriculum_course_path(:curriculum_id => @curriculum, :id => course) %>
        <% end %>
      </ul>
    <% end %>

  </div>

  <div class="content">
    <%= render :partial => 'layouts/messages' %>

    <%= yield %>
  </div>
</div>


</body>
</html>
