<!DOCTYPE html>

<html lang="<%= I18n.locale %>">
<%= render :partial => 'layouts/head' %>

<body>

<%= render :partial => 'layouts/header' %>

  
<div class="page">
  <!--div id="contextHelp">
  </div-->
  <div class="leftNavi">
    <ul>
      <li><%= link_to t(:navi_plan), plan_path %></li>
      <li><%= link_to t(:navi_curriculums), curriculums_path %></li>
    </ul>
    
    <% if @curriculum %>
      <h2><%= @curriculum.name %></h2>

      <ul>
        <li><%= link_to t(:navi_profiles), profiles_path %></li>
        <!-- Profiles belonging to the curriculum -->
        <ul>
          <% @curriculum.profiles.each do |profile| %>
            <li class="<%= profile == @profile ? 'selected' : '' %>"><%= link_to profile.name(I18n.locale), profile_url(:id => profile.id) %></li>
            <!-- Courses belonging to the profile -->
            
            <% if profile == @profile %>
              <ul>
                <% profile.courses.each do |course| %>
                  <li class="<%= course == @course ? 'selected' : '' %>"><%= link_to course.name(I18n.locale), profile_course_url(:profile_id => profile.id, :id => course.id) %>
                <% end %>
              </ul>
            <% end %>
          <% end %>
        </ul>
        
        <li><%= link_to t(:navi_courses), courses_path %></li>
      </ul>
    <% end %>
  </div>

  <div class="content">
    <%= render :partial => 'layouts/messages' %>

    <%= yield %>
  </div>
</div>


</body>
</html>
